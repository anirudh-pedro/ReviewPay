# RevivePay Phase 4 — pinned production dependencies
# No ML/LLM, Redis, Celery, APScheduler, or real payment-provider packages.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
pydantic-settings==2.15.0
SQLAlchemy==2.0.52
alembic==1.14.1
# Modern PostgreSQL v3 driver; SQLite remains the default local/demo/test backend.
psycopg[binary]==3.2.6

# Testing
pytest==8.4.2
# Property-based testing for the deterministic mappers, lifecycle transitions,
# source-binding predicates, and concurrency invariants required to run at least
# 100 generated cases (Requirement 15.11).
hypothesis==6.125.3
